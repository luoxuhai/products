# App Portfolio

A static app portfolio website built with Astro.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

App data is centrally maintained in `src/data/apps.ts`, with no need to request remote APIs at runtime.
